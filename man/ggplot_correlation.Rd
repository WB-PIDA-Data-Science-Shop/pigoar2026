% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{ggplot_correlation}
\alias{ggplot_correlation}
\title{Plot correlation between a numeric outcome and a predictor with regional coloring}
\usage{
ggplot_correlation(data, x, y, group, filename = NULL)
}
\arguments{
\item{data}{A data frame or tibble containing the variables to plot. Must include
columns named by \code{x}, \code{y}, and a \code{region} column for color mapping.}

\item{x}{A string naming the predictor column in \code{data}. Used on the x-axis.}

\item{y}{A string naming the outcome column in \code{data}. Used on the y-axis.}

\item{filename}{Optional string path to save the plot. If \code{NULL}, the plot
is not saved. Passed to \code{ggplot2::ggsave()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a scatter plot with points colored by \code{region} and overlays a dashed
quadratic polynomial regression line. Optionally saves the plot to disk.
}
\details{
\itemize{
\item The smoothing line is a quadratic polynomial fit via \code{method = "lm"}.
\item Colors use \code{scale_color_brewer(palette = "Paired")}.
}
}
\examples{
\dontrun{
library(ggplot2)
library(dplyr)

df <- tibble(
  region = rep(c("North", "South", "East", "West"), each = 25),
  predictor = runif(100),
  outcome = 10 + 5 * predictor + rnorm(100)
)

# Create the plot
p <- ggplot_correlation(df, x = "predictor", y = "outcome")

# Save the plot
ggplot_correlation(df, x = "predictor", y = "outcome", filename = "figs/corr.png")
}

}
