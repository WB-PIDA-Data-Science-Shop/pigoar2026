% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{compute_summary}
\alias{compute_summary}
\title{Compute grouped summary statistics for multiple variables}
\usage{
compute_summary(
  data,
  cols,
  fns = NULL,
  groups = NULL,
  output = c("long", "wide")
)
}
\arguments{
\item{data}{A data frame or tibble containing the variables to summarize.}

\item{cols}{A character vector of variable names to summarize, or a list
with an element \code{vars} (e.g., \code{list(vars = c("var1", "var2"))}).}

\item{fns}{Optional. A specification of the summary functions to apply:
\itemize{
\item \code{NULL} (default): applies all default functions returned by \code{\link[=define_fns]{define_fns()}}.
\item A \strong{character vector} of function names (e.g., \code{c("mean", "sd")})
referring to defaults in \code{\link[=define_fns]{define_fns()}}.
\item A \strong{list} of formulas (e.g., \code{list(mean = ~mean(.x, na.rm = TRUE))})
or a mix of character names and formulas.
}}

\item{groups}{A character vector of grouping variables used for aggregation.}

\item{output}{Character, either \code{"long"} or \code{"wide"}. If \code{"long"}, reshapes
the results into a tidy format with columns \code{indicator} and \code{value}.
Defaults to \code{"long"}.}
}
\value{
A tibble containing grouped summary statistics.
\itemize{
\item If \code{output = "wide"}, each statistic appears as a separate column.
\item If \code{output = "long"}, the data are in tidy (long) format with one row per
group-variable-statistic combination.
}
}
\description{
A flexible wrapper around \code{dplyr::summarise()} and \code{dplyr::across()} that
computes one or more summary statistics for a set of numeric variables,
optionally grouped by one or more categorical variables. Users can choose
from built-in summary functions defined in \code{\link[=define_fns]{define_fns()}} or supply their own.
}
\details{
This function simplifies grouped summaries by handling:
\itemize{
\item Flexible function definitions (\code{fns}) using formula notation \code{~expr(.x)}.
\item Integration with user-defined functions via \code{\link[=define_fns]{define_fns()}}.
\item Optional long/wide reshaping for tidy analysis workflows.
}
}
\section{Default Functions}{

The default functions are defined in \code{\link[=define_fns]{define_fns()}} and typically include:
\code{sum}, \code{mean}, \code{median}, \code{cv}, and \code{cp_ratio}. These can be customized or
extended by modifying \code{define_fns()}.
}

\examples{
df <- tibble::tibble(
  country = rep(c("A", "B"), each = 5),
  year = rep(2020:2021, times = 5),
  income = runif(10, 1000, 2000),
  expenditure = runif(10, 500, 1500)
)

 # Default summaries by country and year
compute_summary(data = df,
                cols = c("income", "expenditure"),
                groups = c("country", "year"),
                fns = "mean")

 # Select specific functions
 compute_summary(data = df,
                cols = c("income"),
                groups = c("country"),
                fns = c("mean", "sd"),
                output = "wide")

 # Custom function
 compute_summary(df,
                cols = "income",
                groups = "country",
                fns = list("cv",
                           q90 = ~quantile(.x, 0.9, na.rm = TRUE)))

}
\seealso{
\code{\link[=define_fns]{define_fns()}}, \code{\link[dplyr:summarise]{dplyr::summarise()}}, \code{\link[tidyr:pivot_longer]{tidyr::pivot_longer()}}
}
