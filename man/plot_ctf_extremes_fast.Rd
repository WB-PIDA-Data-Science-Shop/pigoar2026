% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/country_trends_funs.R
\name{plot_ctf_extremes_fast}
\alias{plot_ctf_extremes_fast}
\title{Plot CTF scores by indicator with extreme country labels}
\usage{
plot_ctf_extremes_fast(
  data,
  family_name,
  shape_var = income_group,
  top_n = 3,
  bottom_n = 3,
  thresholds = c(0, 25, 50, 100),
  threshold_cols = c("red4", "yellow4", "yellow4", "green4"),
  jitter_h = 0.12,
  point_size = 2.5,
  point_alpha = 0.5,
  base_size = 11
)
}
\arguments{
\item{data}{A data frame with at least \code{family_name}, \code{var_name}, \code{country_code}, and \code{score}.}

\item{family_name}{A single string. The \code{family_name} value to filter on.}

\item{shape_var}{Unquoted column mapped to point \code{shape} (default: \code{income_group}).}

\item{top_n}{Integer. Number of top countries to label per indicator (default: 3).}

\item{bottom_n}{Integer. Number of bottom countries to label per indicator (default: 3).}

\item{thresholds}{Numeric vector of x-intercepts for threshold lines (default: c(0,25,50,100)).}

\item{threshold_cols}{Character vector of colors for \code{thresholds} (same length as \code{thresholds}).}

\item{jitter_h}{Numeric. Vertical jitter height (default: 0.12).}

\item{point_size}{Numeric. Point size (default: 2.5).}

\item{point_alpha}{Numeric between 0 and 1. Point alpha (default: 0.5).}

\item{base_size}{Numeric. Base font size for the plot theme (default: 11).}
}
\value{
A ggplot object.
}
\description{
Filters to one \code{family_name}, scales scores to 0–100, labels the top/bottom
countries per indicator, and draws threshold lines at selected cutoffs.
}
