% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/country_trends_funs.R
\name{plot_ctf_time_trends}
\alias{plot_ctf_time_trends}
\title{Plot CTF dynamic scores over time by income group}
\usage{
plot_ctf_time_trends(
  data,
  family,
  title = NULL,
  subtitle = "Average CTF dynamic score by income group",
  y_limits = c(0, 1)
)
}
\arguments{
\item{data}{A data frame with columns: \code{income_group}, \code{family_name}, \code{year}, \code{ctf_score}.}

\item{family}{Character vector or single string. The indicator family/families to plot
(must match value(s) in \code{family_name}). If a vector, returns a named list of plots.}

\item{title}{Character. Plot title. Defaults to the family name. Ignored if \code{family} is a vector.}

\item{subtitle}{Character. Plot subtitle. Defaults to "Average CTF dynamic score by income group".}

\item{y_limits}{Numeric vector of length 2. Y-axis limits. Defaults to c(0, 1).}
}
\value{
A ggplot object if \code{family} is length 1; otherwise a named list of ggplot objects
with file-safe names.
}
\description{
Creates a line plot of average CTF dynamic scores over time, grouped and colored by income group.
Accepts either a single family name or a vector of families.
}
\examples{
# Single plot
plot_ctf_time_trends(plot_dyn, family = "Degree of Integrity")

# Multiple plots as named list
plots <- plot_ctf_time_trends(plot_dyn, family = c("Degree of Integrity", "HRM"))
purrr::iwalk(plots, ~ggsave(paste0(.y, ".png"), .x))
}
