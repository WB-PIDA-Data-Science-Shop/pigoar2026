% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_internal.R
\name{lapply_at}
\alias{lapply_at}
\title{Apply multiple functions to multiple variables (data.table compatible)}
\usage{
lapply_at(var, funs, ...)
}
\arguments{
\item{var}{A vector, list, or data frame-like object (e.g., \code{.SD} inside a
\code{data.table} call) containing the variables to which the functions should
be applied.}

\item{funs}{A character vector of function names (e.g., \code{"mean"}, \code{"sd"}, \code{"sum"})
or a list of function objects to be applied to each element of \code{var}.}

\item{...}{Additional arguments passed to each function call (e.g., \code{na.rm = TRUE}).}
}
\value{
A named list where each element corresponds to a combination of variable
and function (e.g., \code{Sepal.Length_mean}, \code{Sepal.Length_sd}), suitable for
use inside \code{data.table::lapply()}.
}
\description{
A lightweight helper function that applies a list of functions to a set of variables,
mimicking the behavior of \code{dplyr::summarise_at()}, but designed for use within
\code{data.table} syntax. This is useful when you want to compute several summary
statistics (e.g., mean, sd) over a subset of columns defined by \code{.SD} and
\code{.SDcols}.
}
\examples{
library(data.table)
iris_dt <- as.data.table(iris)

# Compute mean and sd for Sepal variables by Species
iris_dt[, lapply_at(.SD, c("mean", "sd"), na.rm = TRUE),
        .SDcols = patterns("^Sepal"),
        by = Species]

}
\seealso{
\code{\link[dplyr:summarise_all]{dplyr::summarise_at()}}
}
